name: Refresh VengeStats Cache
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Refresh cache
        run: curl -X POST "${{ secrets.VENGESTATS_CRON_URL }}"
